#pragma once

#include <vector>

// Returns the sum of the first n elements of vector A using iteration.
int iterativeLinearSum(const std::vector<int>& A, int n);

// Returns the sum of the first n elements of vector A using recursion.
int recursiveLinearSum(const std::vector<int>& A, int n);
